import { withBarkContext } from "../_context"

describe("opCancelAppointment", () => {
  it("WIP: should return ERROR_APPOINTMENT_NOT_FOUND when the appointment does not exist", async () => {
    await withBarkContext(async ({context}) => {})
  })
  it("WIP: should return ERROR_NOT_ALLOWED when the actorVetId is not the vet of the appointment", async () => {
    await withBarkContext(async ({context}) => {})
  })
  it("WIP: should cancel the appointment", async () => {
    await withBarkContext(async ({context}) => {})
  })
})
